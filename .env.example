# Скопируй этот файл в .env и вставь токен бота.
# .env уже в .gitignore — в репозиторий он не попадёт.
# tg.py / send_issue_with_pdf.py читают токен отсюда, поэтому его
# больше НЕ нужно передавать инлайном (export TG_BOT_TOKEN=...) в команде.
TG_BOT_TOKEN=
